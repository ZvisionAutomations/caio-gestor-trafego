# Caio — Gestor de Tráfego Raiz Vital
# Python 3.11+

# Core Framework
agno>=1.0.0

# LLM — OpenRouter via SDK OpenAI-compatible (única chave, múltiplos modelos)
openai>=1.0.0

# Meta Ads SDK (oficial)
facebook-business>=19.0.0

# HTTP Client
httpx>=0.27.0

# Config & Validation
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Scheduler
apscheduler>=3.10.0

# Retry
tenacity>=8.3.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
ruff>=0.6.0
mypy>=1.10.0

# Logging
structlog>=24.1.0

# Nota: anthropic e google-genai removidos — OpenRouter roteia para qualquer
# modelo via openai SDK. Adicionar aqui se precisar de SDK direto no futuro.
